#include <iostream>
#include <cstring>
using namespace std;
int main()
{
    string s1 = "HelloWorld";
    // char str[10];

    // s1.copy(str, s1.length());
    // str[10]='\0';  // Since no null cahr is copied so we get junk value if we use len.
    // cout<<str<<endl;
    // if ((s1.find("H"))<=s1.length())
    // {
    //     cout << s1.find("H") << endl;
    // }
    // else
    //     cout << "not Found" << endl;

    return 0;
}

/*
s1.copy(*char_str,length of char from s1)
s1.find("")
s1.rfind("")
s1.find_first_of()
s1.find_last_of()
s1.substr(starting point, no of chars)
s1.compare(str)

*/